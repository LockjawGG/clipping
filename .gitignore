# dependencies
node_modules/

# next.js
/.next/
/out/
next-env.d.ts
*.tsbuildinfo

# environment
.env
.env.local

# prisma
/prisma/migrations/dev.db*

# local storage / scratch / build artifacts
.storage/
.tmp/
# Piper voice models: tens of megabytes of downloaded weights, reproducible
# from huggingface.co/rhasspy/piper-voices rather than worth versioning.
.voices/
dist/
*.log

# desktop build output
dist-desktop

# bundled third-party binaries for the portable build (large, fetched not authored)
vendor/

# Local database backups (Settings > Backup)
/backups/
